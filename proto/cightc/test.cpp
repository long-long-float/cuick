#include<iostream>
#include<vector>
#include<map>
#include<algorithm>
vector<T> gets(int n)
{
    vector<T> ret(n);
    n.times()    {
        cin >> jp.long_long_float.cuick.ast.ArefNode@b56efe;
    };
;
    ret;
}

int main(int argc, char** argv)
{
    return 0;
}

