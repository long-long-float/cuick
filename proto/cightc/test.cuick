/*
extend int int.times() {
    for(i : 0...this) yield(i);
}

$T gets() {
    $T ret;
    cin >> ret;
    ret;
}

vector($T) gets(int n) {
    vector($T) ret(n);
    n.times() { cin >> ret[it]; };
    ret;
}

extend void $CONT.sort() {
    sort(this.begin(), this.end());
}
*/

void puts(int i) {
    std::cout << i << std::endl;
}

int ary[] = {1, 2, 3, 4, 5};
/*for(i : ary) puts(i ** 2);
for(i : ary : 2..4) puts(i);
for(i : ary : 3) puts(i);*/

int counter = 10;
for(i : ary){
    for(j : ary){
        puts(counter);
    }
}