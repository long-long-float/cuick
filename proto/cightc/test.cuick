/*
extend int int.times() {
    for(i : 0...this) yield(i);
}

$T gets() {
    $T ret;
    cin >> ret;
    ret;
}

vector($T) gets(int n) {
    vector($T) ret(n);
    n.times() { cin >> ret[it]; };
    ret;
}

extend void $CONT.sort() {
    sort(this.begin(), this.end());
}
*/
//入力を二乗して出力するプログラム
@test {
    in: "in.txt"
    out: "out.txt"
}
int n;
@while(true)
@input { n! }
unless(n == 2) puts(n ** 2);