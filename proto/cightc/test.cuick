/*
extend int int.times() {
    for(i : 0...this) yield(i);
}

$T gets() {
    $T ret;
    cin >> ret;
    ret;
}

vector($T) gets(int n) {
    vector($T) ret(n);
    n.times() { cin >> ret[it]; };
    ret;
}

extend void $CONT.sort() {
    sort(this.begin(), this.end());
}
*/
int i = 0;
puts(i);